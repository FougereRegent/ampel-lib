commit-msg:
  scripts:
    "message-checker.sh":
      runner: bash

checkout:
  commands:
    clean:
      run: make clean

pre-commit:
  commands:
    reset-header:
      run: git restore ./ampel-lib.h
